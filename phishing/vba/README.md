stager_win32.odt
- not functional
- overflow issue, cannot find 64bits data type
- pointers not supported

stager.docm
- add MyMacro to DocumentOpen() to run the macro on doc open
- supports xor and caesar decoding
- change the 'decryption' and 'key' variables